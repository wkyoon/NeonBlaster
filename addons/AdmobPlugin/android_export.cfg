; AdMob export 설정 — export 시 AndroidManifest 의 com.google.android.gms.ads.APPLICATION_ID 로 들어간다.
;
; ⚠️ 이 파일이 없으면 앱이 **시작하자마자 죽는다.**
;    AdMob SDK 의 MobileAdsInitProvider 가 프로세스 시작 시 매니페스트에서 앱 ID를 읽는데,
;    코드에서 Admob 노드에 ID를 넣는 것은 그보다 한참 뒤라 늦다.
;    실제 겪은 크래시: "java.lang.IllegalStateException: Invalid application ID"
;
; ⚠️ 지금 값은 Google **테스트 앱 ID** 다. 출시 전에 실제 ID 로 교체할 것.
;    개발 중에 실제 ID 를 넣고 자기 광고를 누르면 계정이 정지된다.
[General]
is_real=false

[Debug]
app_id="ca-app-pub-3940256099942544~3347511713"

[Release]
app_id="ca-app-pub-3940256099942544~3347511713"
